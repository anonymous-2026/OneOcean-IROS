"""External underwater/ocean scene assets (local-cache fetch + conversion).

Policy:
- Large third-party assets must not be committed to Git.
- Fetch into a local cache under `runs/_cache/<provider>/<scene_id>/`.
"""

