"""External underwater/ocean scene asset helpers (download + conversion).

Policy:
- Large third-party assets must not be committed to Git.
- Fetch into a local cache under `runs/_cache/external_scenes/<scene_id>/`.
"""

